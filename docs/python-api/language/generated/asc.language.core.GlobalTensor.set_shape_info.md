# asc.language.core.GlobalTensor.set_shape_info

#### GlobalTensor.set_shape_info(shape_info: ShapeInfo) → None

设置GlobalTensor的shape信息。

**对应的Ascend C函数原型**

```c++
__aicore__ inline void SetShapeInfo(const ShapeInfo& shapeInfo)
```

**参数说明**

- shape_info：ShapeInfo结构体。
