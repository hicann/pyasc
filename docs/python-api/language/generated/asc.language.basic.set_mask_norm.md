# asc.language.basic.set_mask_norm

### asc.language.basic.set_mask_norm() → None

设置掩码模式为 Normal 模式，这是掩码操作的默认模式。

**对应的 Ascend C 函数原型**

```c++
__aicore__ inline void SetMaskNorm();
```

**参数说明**

- 无

**约束说明**

- 无

**调用示例**

```python
asc.set_mask_norm()
```
